What is HashSet in Java?
Why do we use HashSet?
What is the difference between HashSet and HashMap?
What is the difference between HashSet and LinkedHashSet?
What is the difference between HashSet and TreeSet?
Can HashSet store duplicate elements?
Can HashSet contain null?
What happens when you add the same element twice?
What is the return type of add()?
What is the return type of contains() and remove()?
How does HashSet work internally?
How is HashSet internally related to HashMap?
Why does HashSet use HashMap internally?
What happens internally when you call set.add(element)?
What is the role of hashCode() in HashSet?
What is the role of equals() in HashSet?
Why should equals() and hashCode() be overridden together?
What happens if two different objects have the same hashCode()?
What is a hash collision in HashSet?
How does HashSet handle hash collisions?
What is the default capacity of the underlying HashMap?
What is the default load factor?
What is the relationship between capacity, load factor, and threshold?
What happens when the underlying HashMap reaches its threshold?
What is resizing in HashSet?
What is rehashing in the underlying HashMap?
Why is the underlying HashMap capacity generally maintained as a power of two?
What changed in the internal implementation after Java 8?
What is treeification?
What are TREEIFY_THRESHOLD, UNTREEIFY_THRESHOLD, and MIN_TREEIFY_CAPACITY?
What is the time complexity of add(), remove(), and contains()?
What is the worst-case performance of HashSet before and after Java 8?
Is HashSet thread-safe?
What is the difference between HashSet and a synchronized set?
What is the difference between HashSet and a concurrent set?
What is ConcurrentModificationException and why can it occur with HashSet?
What happens if a mutable object stored in a HashSet is modified?
How would you design a good custom class to be stored in a HashSet?
How would you troubleshoot poor HashSet performance caused by excessive hash collisions?
Explain the complete internal working of HashSet from add(element) to storing the element internally.
